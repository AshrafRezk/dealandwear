import React from 'react';

const Saved = () => {
  return (
    <div style={{ padding: '2rem', textAlign: 'center', background: '#f8f9fa', minHeight: '100vh' }}>
      <h1>Saved Items</h1>
      <p>Your bookmarked styling inspiration will appear here.</p>
    </div>
  );
};

export default Saved;
